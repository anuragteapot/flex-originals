version: '2'
services:
  flex-originals:
    container_name: flex-originals
    image: flex-originals
    restart: always
    build: .
    ports:
      - '80:3333'
    links:
      - mongo

  mongo:
    container_name: mongo
    image: mongo
    ports:
      - '27017:27017'
